query GetLeadTimeByProduct(
  $itemId: String
  $sellerId: String
  $quantity: Int
  $postalCode: String
) {
  leadTime(skuId: $itemId, sellerId: $sellerId, quantity: $quantity, postalCode: $postalCode) {
    itemId
    leadTime
  }
}
