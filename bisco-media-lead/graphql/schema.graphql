type Document {
  productRefId: String,
  id: String,
  linkUrl: String,
  linkText: String,
  mediaType: String,
  locale: String
}

type Query {
  getMediaLeadDocuments(where: String):[Document]
}

